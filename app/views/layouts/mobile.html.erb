<!DOCTYPE html>
<html>
<head>
  <title>Aether - Mobile</title>
  <%= stylesheet_link_tag 'normalize.css', 'scaffold.css', 'screen.css', 'mobile.css' %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>
    <div id="fb-root"></div>
    <script>
     //  window.fbAsyncInit = function() {
     //     FB.init({
     //       appId      : '134448519991788', // App ID
     //       status     : true, // check login status
     //       cookie     : true, // enable cookies to allow the server to access the session
     //       oauth      : true, // enable OAuth 2.0
     //       xfbml      : true  // parse XFBML
     //     });
     // 
     //     // Additional initialization code here
     //     
     //     FB.getLoginStatus(function(response) {
     //       if (response.authResponse) {
     //           FB.api('/me', function(response) {
     //               $("#name").text(response.name)
     //             });
     //         // logged in and connected user, someone you know
     //       } else {
     //           FB.Event.subscribe('auth.login', function(response) {
     //               FB.api('/me', function(response) {
     //                   $("#name").text(response.name)
     //                 });
     //             // do something with response
     //           });
     //           FB.login();
     //         // no user session available, someone you dont know
     //       }
     //     });
     // 
     // };
     // 
     //   // Load the SDK Asynchronously
     //   (function(d){
     //      var js, id = 'facebook-jssdk'; if (d.getElementById(id)) {return;}
     //      js = d.createElement('script'); js.id = id; js.async = true;
     //      js.src = "//connect.facebook.net/en_US/all.js";
     //      d.getElementsByTagName('head')[0].appendChild(js);
     //    }(document));
       

    </script>
    <header style="text-align:center;">
        <div class="headWrap" style="width:750px;margin:0 auto;text-align:left;height:85px;background:transparent url('/images/yc_mobile_bg.jpg') no-repeat left center">
            <div class="left" style="float:left">
                <h1>YC COFFEE</h1>
                <span class="mv">MUSICVIBE</span>
            </div>
            <div class="right" style="float:right">
                <span id="name"></span>
                FB
            </div>
        </div>
        
    </header>
<%= yield %>

</body>
</html>
